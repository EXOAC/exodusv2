export interface YouTubeVideo {
  id: string;
  url: string;
}

export interface ThumbnailQuality {
  width: number;
  url: string;
}

export interface VideoMetadata {
  id: string;
  thumbnailUrl: string;
  embedUrl: string;
}